# 分层断言策略实现计划

## 背景

当前 AI 生成测试用例时，对 `message` 等文本字段做精确匹配（如期望 `"成功"` vs 实际 `"success"`），导致语义相同但文字不同的断言失败。需要按照分层断言原则进行优化。

## 断言设计原则

- **分层验证**：协议层（HTTP 状态码）→ 基础层（code 字段）→ 业务层（data 字段存在性）
- **关注业务价值**：断言体现业务规则，非关键字段适当放宽
- **可读性优先**：断言失败信息直接定位问题
- **避免过度断言**：`message` 等提示语字段不做精确匹配

## 修改方案

---

### 1. AI 提示词优化 - 生成更聪明的断言

#### [MODIFY] [ai_case_generator.py](file:///d:/testc/aitesting-api/services/ai-processing/services/ai_case_generator.py)

修改 `SYSTEM_PROMPT` 中第 4 条规则（业务级断言，约第 79-88 行），加入分层断言指导：

```diff
-4. **业务级断言 expected_template.response_body（必填，不得省略）**
-   - 所有用例（包括正向、边界、健壮、安全）都必须填写 `expected_template.response_body`。
-   - 禁止返回 `response_body: {}` 这样的空对象，至少要包含 1~3 个关键字段，例如：
-     - 登录成功：{"code": 0, "message": "成功"} 或等价语义；
-     - 密码错误/空密码：{"code": 401, "message": "密码错误"}；
-     - 无效手机号格式：{"code": 400, "message": "无效手机号"}；
-     - 缺少必填参数：{"code": 400} 或 {"code": 400, "message": "参数错误"}。
+4. **业务级断言 expected_template.response_body（必填，遵循分层断言原则）**
+   - 所有用例都必须填写 `expected_template.response_body`，但需要遵循「分层断言」策略：
+
+   **第一层 - 状态码断言（精确匹配）**：
+   - `code`/`status`/`errcode` 等数值型状态字段 → 必须给出精确期望值
+   - 示例：{"code": 0} 或 {"code": 401}
+
+   **第二层 - 关键业务数据断言（存在性检查）**：
+   - `data`/`token`/`userId` 等业务数据字段 → 使用 "非空" 表示"该字段必须存在且非空"
+   - 示例：{"data": "非空"} 或 {"token": "非空"}
+   - 不要对 data/token 等字段猜测具体值（如 "some_valid_token"），因为这些值是动态的
+
+   **第三层 - 提示语字段（不做精确匹配）**：
+   - `message`/`msg`/`errmsg` 等提示语字段 → **不要放入 response_body**
+   - 原因：提示语可能是中文"成功"也可能是英文"success"，精确匹配必然失败
+   - 如需提示语断言，在 description 中说明即可
+
+   **正确示例**：
+   - 登录成功：{"code": 0, "data": "非空"}（不断言 message 和 token 具体值）
+   - 密码错误：{"code": 401}（只断言状态码，不断言 message）
+   - 缺少参数：{"code": 400}
```

---

### 2. 后端断言逻辑优化 - 对 message 字段和动态字段宽松匹配

#### [MODIFY] [main_sqlite.py](file:///d:/testc/aitesting-api/services/ai-processing/main_sqlite.py)

在断言比较逻辑中（约第 1750-1790 行），增加两层优化：

**A) message 字段增加跨语言等价判断（约第 1750 行后）**：
```python
# 常见中英文等价词汇映射
SEMANTIC_EQUIVALENTS = [
    {"成功", "success", "ok", "succeed", "操作成功"},
    {"失败", "fail", "failed", "failure", "error", "操作失败"},
    {"参数错误", "param error", "parameter error", "invalid params", "bad request"},
    {"未授权", "unauthorized", "未授权", "no auth"},
    {"禁止访问", "forbidden", "禁止", "no permission"},
    {"不存在", "not found", "不存在", "no data"},
    {"密码错误", "wrong password", "invalid password", "password error"},
]

def _semantic_match(expected: str, actual: str) -> bool:
    """语义等价匹配：检查两个值是否在同一组等价词中"""
    e_lower = expected.strip().lower()
    a_lower = actual.strip().lower()
    if e_lower == a_lower:
        return True
    for equiv_set in SEMANTIC_EQUIVALENTS:
        lower_set = {s.lower() for s in equiv_set}
        if e_lower in lower_set and a_lower in lower_set:
            return True
    return False
```

在第 1766 行现有 message 模糊匹配逻辑之后，加入语义等价判断：
```python
# 已有的包含匹配之后，再尝试语义等价匹配
if not match and normalized_field_for_msg in MESSAGE_FIELDS:
    if _semantic_match(expected_val, actual):
        match = True
```

**B) 对 "非空" 关键词的断言，改为存在性检查（约第 1720 行后）**：

已有的 integrity 分类逻辑（line 1777-1781）中存在对 "非空" 的检查，但它只在 `expected_val is None` 时触发 category 标记，并没有改变 `match` 判断逻辑。需要修改为：

```python
# 在比较逻辑中，如果期望值是"非空"类关键词，则改为存在性检查
NON_EMPTY_KEYWORDS = {"非空", "not null", "not_null", "not_empty", "存在", "exists"}
text_expected_check = str(expected_val).strip() if expected_val is not None else ""

if text_expected_check in NON_EMPTY_KEYWORDS:
    # 存在性断言：只要实际值非 None 且非空字符串即可
    match = actual is not None and str(actual).strip() not in ("", "null", "None")
    category = "integrity"
else:
    # 正常比较逻辑（保留现有代码）
    ...
```

---

## 验证计划

### 手动验证
1. 重启后端服务
2. 重新生成测试用例（AI 生成），检查 `expected_template.response_body` 是否遵循分层原则
3. 执行测试用例，验证：
   - `code` 字段仍然精确匹配
   - `message` 字段不再因为中英文差异而失败
   - `data`/`token` 设为 "非空" 时，只要存在即通过

---

## 实施状态

✅ **已完成** - 2026-02-11

### 修改的文件
1. `services/ai-processing/services/ai_case_generator.py` - AI 提示词优化
2. `services/ai-processing/main_sqlite.py` - 后端断言比较逻辑优化
