# 场景用例执行与分析实现说明

## 一、实现概述

在场景用例执行接口中，添加了**阶段3：测试执行与结果分析**功能，包括：
1. **子阶段1：测试执行** - 执行所有测试步骤
2. **子阶段2：结果分析** - 分析业务状态码，判断是否通过

**重要**：此实现**不影响**前面的场景用例生成步骤（阶段1：场景理解、阶段2：场景编排）。

---

## 二、代码修改位置

### 1. 新增函数：`_analyze_execution_results`

**位置**：`services/ai-processing/main_sqlite.py` 第2538行之前

**功能**：
- 分析每个步骤的执行结果
- 检查HTTP状态码（2xx范围）
- 检查业务状态码（`code`字段是否为0）
- 提取错误信息（`message`字段）
- 生成分析报告

**判断规则**：
- **通过条件**：HTTP状态码在2xx范围 **且** 业务状态码为0（或不存在业务状态码时，HTTP状态码在2xx范围）
- **失败条件**：HTTP状态码不在2xx范围 **或** 业务状态码不为0

**返回格式**：
```json
{
  "overall_status": "passed" | "failed",
  "total_steps": 3,
  "passed_steps": 2,
  "failed_steps": 1,
  "analysis": [
    {
      "step_order": 1,
      "api_path": "/api/login",
      "status": "passed",
      "http_status": 200,
      "business_code": 0,
      "message": "登录成功",
      "failure_reason": null,
      "response": {...}
    },
    {
      "step_order": 2,
      "api_path": "/api/orders",
      "status": "failed",
      "http_status": 200,
      "business_code": 4200,
      "message": "门店授权码无效，请联系相关人员",
      "failure_reason": "业务状态码错误：4200；错误信息：门店授权码无效，请联系相关人员",
      "response": {...}
    }
  ],
  "summary": "场景执行失败：1个步骤失败，2个步骤通过"
}
```

### 2. 修改执行接口：`/api/v1/executions`

**位置**：`services/ai-processing/main_sqlite.py` 第2946行

**修改内容**：
1. 执行测试步骤（原有逻辑）
2. **新增**：调用 `_analyze_execution_results` 分析结果
3. **修改**：判断整体状态时，使用分析结果（所有步骤业务状态码都为0才算通过）
4. **修改**：知识图谱学习条件（只有所有步骤都通过才学习）
5. **新增**：返回分析结果

**修改前**：
```python
step_results = await _run_steps(steps, req.base_url)
final_status = "success" if all(s.get("success", False) for s in step_results) else "failed"
# ... 保存执行记录
return {"id": exec_id, "status": final_status, "results": step_results}
```

**修改后**：
```python
# ========== 阶段3：测试执行 ==========
step_results = await _run_steps(steps, req.base_url)

# ========== 阶段3：结果分析 ==========
analysis_result = _analyze_execution_results(step_results)
all_passed = analysis_result["overall_status"] == "passed"
final_status = "success" if all_passed else "failed"

# 只有所有步骤都通过才学习到知识图谱
if project_id_for_kg and _kg and all_passed:
    _learn_steps_to_kg(...)

# ... 保存执行记录
return {
    "id": exec_id,
    "status": final_status,
    "results": step_results,
    "analysis": analysis_result  # 新增：分析结果
}
```

---

## 三、业务状态码提取逻辑

### 支持的字段名

**业务状态码字段**：
- `code`
- `errcode`
- `retCode`
- `status`
- `ret`
- `error_code`
- `statusCode`

**错误信息字段**：
- `message`
- `msg`
- `errmsg`
- `info`
- `error`
- `desc`
- `description`

### 提取逻辑

1. **优先从响应体根级别提取**
   ```json
   {
     "code": 0,
     "message": "成功"
   }
   ```

2. **如果根级别没有，从data中提取**
   ```json
   {
     "data": {
       "code": 0,
       "message": "成功"
     }
   }
   ```

3. **判断规则**
   - 如果存在业务状态码字段，必须为0才算通过
   - 如果不存在业务状态码字段，只检查HTTP状态码（2xx范围）

---

## 四、不影响前面的步骤

### 保证措施

1. **独立的函数**：`_analyze_execution_results` 是独立函数，不修改场景用例生成逻辑
2. **执行后分析**：分析逻辑在**执行完成后**进行，不影响执行过程
3. **只读操作**：分析函数只读取执行结果，不修改测试用例数据
4. **不影响生成**：场景用例生成（阶段1和阶段2）完全不受影响

### 场景用例生成流程（未改变）

```
阶段1：场景理解（NLU Agent）
    ↓
阶段2：场景编排（ScenarioParser）
    ├─ API检索
    ├─ 知识图谱增强
    ├─ AI编排生成
    └─ 步骤增强
    ↓
保存到test_cases表
```

### 执行与分析流程（新增）

```
读取test_cases表
    ↓
阶段3子阶段1：测试执行
    ├─ 执行所有步骤
    └─ 收集执行结果
    ↓
阶段3子阶段2：结果分析（新增）
    ├─ 检查HTTP状态码
    ├─ 检查业务状态码（code字段）
    ├─ 提取错误信息（message字段）
    └─ 生成分析报告
    ↓
判断整体状态
    ├─ 全部通过 → 学习依赖关系到知识图谱
    └─ 有失败 → 返回分析结果，等待自愈
```

---

## 五、自愈集成

### 自愈接口（已存在，无需修改）

1. **分析失败原因**：`POST /api/v1/heal/analyze`
   - 输入：`execution_id`
   - 输出：失败类型、根因、是否可自愈、修复建议

2. **应用修复**：`POST /api/v1/heal/apply`
   - 输入：`test_case_id`、`execution_id`
   - 输出：修复结果

### 使用流程

```
执行场景用例
    ↓
返回分析结果（包含失败步骤信息）
    ↓
前端判断：有失败步骤？
    ├─ 是 → 调用 /api/v1/heal/analyze 分析失败原因
    │   ↓
    │   判断是否可自愈？
    │   ├─ 是 → 调用 /api/v1/heal/apply 应用修复
    │   │   ↓
    │   │   修复后重新执行
    │   └─ 否 → 提示人工介入
    └─ 否 → 显示成功，学习到知识图谱
```

---

## 六、返回数据格式

### 执行接口返回（新增analysis字段）

```json
{
  "id": 123,
  "status": "failed",
  "results": [
    {
      "step_order": 1,
      "url": "http://example.com/api/login",
      "method": "POST",
      "status_code": 200,
      "response": {
        "code": 0,
        "message": "登录成功",
        "data": {
          "token": "..."
        }
      },
      "success": true
    },
    {
      "step_order": 2,
      "url": "http://example.com/api/orders",
      "method": "POST",
      "status_code": 200,
      "response": {
        "code": 4200,
        "message": "门店授权码无效，请联系相关人员"
      },
      "success": false
    }
  ],
  "analysis": {
    "overall_status": "failed",
    "total_steps": 2,
    "passed_steps": 1,
    "failed_steps": 1,
    "analysis": [
      {
        "step_order": 1,
        "api_path": "/api/login",
        "status": "passed",
        "http_status": 200,
        "business_code": 0,
        "message": "登录成功",
        "failure_reason": null
      },
      {
        "step_order": 2,
        "api_path": "/api/orders",
        "status": "failed",
        "http_status": 200,
        "business_code": 4200,
        "message": "门店授权码无效，请联系相关人员",
        "failure_reason": "业务状态码错误：4200；错误信息：门店授权码无效，请联系相关人员"
      }
    ],
    "summary": "场景执行失败：1个步骤失败，1个步骤通过"
  }
}
```

---

## 七、测试建议

### 测试场景1：全部通过

**输入**：所有步骤的业务状态码都为0

**预期**：
- `analysis.overall_status` = "passed"
- `analysis.passed_steps` = 总步骤数
- `analysis.failed_steps` = 0
- 学习到知识图谱

### 测试场景2：部分失败

**输入**：部分步骤的业务状态码不为0

**预期**：
- `analysis.overall_status` = "failed"
- `analysis.failed_steps` > 0
- 失败步骤的 `failure_reason` 包含错误信息
- 不学习到知识图谱

### 测试场景3：HTTP状态码错误

**输入**：HTTP状态码不在2xx范围

**预期**：
- `analysis.overall_status` = "failed"
- 失败步骤的 `failure_reason` 包含"HTTP状态码错误"

### 测试场景4：无业务状态码字段

**输入**：响应中没有code等业务状态码字段

**预期**：
- 只检查HTTP状态码
- HTTP状态码在2xx范围 → 通过
- HTTP状态码不在2xx范围 → 失败

---

## 八、注意事项

1. **不影响场景用例生成**：此实现只修改执行接口，不影响场景用例生成逻辑
2. **向后兼容**：如果响应中没有业务状态码字段，只检查HTTP状态码（保持原有逻辑）
3. **知识图谱学习**：只有所有步骤都通过（业务状态码为0）才学习到知识图谱
4. **自愈触发**：自愈需要前端主动调用，不会自动触发

---

## 九、总结

✅ **已实现**：
- 阶段3子阶段1：测试执行（原有功能）
- 阶段3子阶段2：结果分析（新增功能）
  - 检查业务状态码（code字段）
  - 提取错误信息（message字段）
  - 生成分析报告

✅ **不影响**：
- 阶段1：场景理解
- 阶段2：场景编排

✅ **自愈集成**：
- 分析接口：`/api/v1/heal/analyze`（已存在）
- 应用接口：`/api/v1/heal/apply`（已存在）
- 前端可调用自愈接口修复失败步骤
