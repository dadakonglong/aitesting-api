# 知识图谱在单接口依赖解析中的使用详解

## 一、概述

在**单接口测试执行前**，系统会**优先查询知识图谱**，检查该接口是否有前置依赖（如登录接口）。如果有高置信度的前置依赖，会直接插入前置步骤，避免走RAG+AI流程，减少延迟和错误。

---

## 二、知识图谱的工程实现

### 2.1 技术架构

知识图谱采用**轻量级实现**，使用 `NetworkX` 替代 Neo4j，无需额外的图数据库服务：

- **存储方式**：使用 `pickle` 序列化到 `.pkl` 文件
- **图结构**：有向图（DiGraph），节点表示API，边表示依赖关系
- **节点ID格式**：`{project_id}:{method}:{path}`，例如 `"default-project:POST:/api/login"`

### 2.2 核心类：LightweightKnowledgeGraph

**文件位置**：`services/ai-processing/lightweight_services.py`

**关键方法**：

1. **`get_predecessors(api_id, min_confidence=0.5, limit=5)`**
   - 获取「谁在调用当前 API 之前」的前置依赖（入边）
   - 用于执行时补全 param_mappings 或单接口流水线解析依赖链
   - 返回按置信度排序的前置依赖列表

2. **`add_dependency(from_api, to_api, field_mapping, success=True)`**
   - 添加API依赖关系
   - 记录字段映射关系
   - 根据执行结果更新置信度（成功增加，失败减少）

3. **`get_edges_for_prompt(project_id, apis_for_prompt, min_confidence=0.5, limit=50)`**
   - 获取与候选API相关的依赖边，用于场景生成时的上下文增强

---

## 三、单接口依赖解析流程

### 3.1 调用入口

**函数**：`_resolve_dependencies()`

**位置**：`services/ai-processing/main_sqlite.py` 第165行

**调用时机**：在单接口测试执行前，自动调用

**代码位置**：
```python
# 在单接口执行流水线中（main_sqlite.py 第1671行）
steps = await _resolve_dependencies(
    steps, plan, project_id, DB_PATH, ai_client
)
```

### 3.2 完整流程

```
单接口测试执行前
    ↓
【步骤1】检查缓存
    - 如果该接口的依赖已解析过，直接使用缓存
    ↓
【步骤2】跳过登录类接口
    - 如果目标接口是登录/认证类，直接跳过依赖分析
    ↓
【步骤2.5】★ 知识图谱查询（优先）
    - 构建节点ID: {project_id}:{method}:{path}
    - 调用 _kg.get_predecessors() 查询前置依赖
    - 如果找到高置信度（≥0.5）的前置依赖：
        → 直接构建依赖链
        → 缓存结果
        → 返回（跳过后续RAG+AI流程）
    ↓
【步骤3】RAG检索（知识图谱无结果时）
    - 使用关键词检索关联接口
    - 根据接口特征添加语义查询（如需要鉴权则查询"登录"）
    ↓
【步骤4】AI分析（RAG有结果时）
    - 将目标接口和关联接口传给AI
    - AI分析是否需要前置依赖
    - 生成依赖链
    ↓
【步骤5】应用依赖链
    - 将依赖链转换为真实的执行步骤
    - 插入到原始steps之前
    - 配置param_mappings
```

### 3.3 关键代码实现

#### 3.3.1 知识图谱查询（优先执行）

```python
# 2.5 知识图谱：若有前置依赖则直接构建链并返回（优先于 RAG+AI）
if _kg:
    try:
        # 构建节点ID：格式为 {project_id}:{method}:{path}
        node_id = f"{project_id}:{method}:{path}"
        
        # 查询前置依赖（入边），置信度阈值0.5，最多返回5个
        preds = _kg.get_predecessors(node_id, min_confidence=0.5, limit=5)
        
        if preds:
            # 构建依赖链
            chain = [
                {
                    "api_path": p.get("path", ""), 
                    "api_method": (p.get("method") or "GET").upper(), 
                    "reason": "知识图谱前置"
                }
                for p in preds
            ]
            
            # 缓存结果
            _DEP_CACHE[cache_key] = chain
            
            # 直接应用依赖链，跳过后续RAG+AI流程
            return _apply_dep_chain(steps, chain, project_id, db_path)
    except Exception as _e:
        # 知识图谱查询失败不影响主流程，继续走RAG+AI
        _dlog(f"知识图谱查询跳过: {_e}")
```

#### 3.3.2 get_predecessors 方法实现

```python
def get_predecessors(
    self,
    api_id: str,
    min_confidence: float = 0.5,
    limit: int = 5,
) -> List[Dict]:
    """
    获取「谁在调用当前 API 之前」的前置依赖（入边）。
    用于执行时补全 param_mappings 或单接口流水线解析依赖链。
    """
    if api_id not in self.graph:
        return []
    
    out = []
    # 遍历所有指向当前节点的边（前置依赖）
    for from_api in self.graph.predecessors(api_id):
        edge = self.graph[from_api][api_id]
        count = edge.get("count") or 0
        if count <= 0:
            continue
        
        # 计算置信度：成功次数 / 总次数
        success_count = edge.get("success_count", 0)
        confidence = success_count / count
        
        # 过滤低置信度边
        if confidence < min_confidence:
            continue
        
        # 获取前置API的节点属性
        node_attrs = dict(self.graph.nodes[from_api])
        out.append({
            "from_api": from_api,
            "field_mapping": edge.get("field_mapping") or {},
            "confidence": round(confidence, 2),
            "path": node_attrs.get("path", ""),
            "method": node_attrs.get("method", "GET"),
        })
    
    # 按置信度降序排序
    out.sort(key=lambda x: (-x["confidence"],))
    return out[:limit]
```

#### 3.3.3 应用依赖链

```python
def _apply_dep_chain(target_steps: List[Dict], chain: List[Dict], project_id: str, db_path: str) -> List[Dict]:
    """将依赖链转化为真实的执行步骤"""
    if not chain:
        return target_steps
    
    # 从数据库查询依赖接口的详细信息
    conn = sqlite3.connect(db_path)
    cursor = conn.cursor()
    
    new_steps = []
    step_order_ptr = 1
    
    # 为每个依赖接口创建执行步骤
    for dep in chain:
        d_path = dep.get("api_path")
        d_method = dep.get("api_method", "POST").upper()
        
        # 从数据库查询接口定义
        cursor.execute(
            "SELECT request_body, base_url, headers FROM apis WHERE project_id = ? AND path = ? AND method = ?",
            (project_id, d_path, d_method)
        )
        row = cursor.fetchone()
        
        if row:
            # 创建前置步骤
            new_step = {
                "step_order": step_order_ptr,
                "api_path": d_path,
                "api_method": d_method,
                "params": {},  # 根据接口定义填充
                "headers": {},
                "param_mappings": [],  # 前置步骤通常无依赖
                "description": f"[前置依赖] {dep.get('reason', '')}"
            }
            new_steps.append(new_step)
            step_order_ptr += 1
    
    # 更新原始步骤的step_order
    for step in target_steps:
        step["step_order"] = step_order_ptr
        step_order_ptr += 1
    
    # 合并：前置步骤 + 原始步骤
    return new_steps + target_steps
```

---

## 四、知识图谱的数据结构

### 4.1 节点（Node）

**节点ID格式**：`{project_id}:{method}:{path}`

**节点属性**：
```python
{
    "node_type": "api",
    "path": "/api/login",
    "method": "POST",
    "name": "用户登录",
    # ... 其他API属性
}
```

### 4.2 边（Edge）

**边表示依赖关系**：`from_api → to_api`（从from_api调用后，才能调用to_api）

**边属性**：
```python
{
    "field_mapping": {
        "Authorization": "data.token",  # 字段映射关系
        "userId": "data.userId"
    },
    "count": 10,              # 总调用次数
    "success_count": 9,       # 成功次数
    "confidence": 0.9,        # 置信度 = success_count / count
    "source_type": "execution",  # 来源类型
    "source_id": "test_case_123",  # 来源ID
    "created_at": "2024-01-01T00:00:00",
    "last_used": "2024-01-15T12:00:00"
}
```

### 4.3 示例图结构

```
节点: "default-project:POST:/api/login"
    ↓ (边：field_mapping={"Authorization": "data.token"}, confidence=0.9)
节点: "default-project:GET:/api/user/profile"
    ↓ (边：field_mapping={"sessionId": "data.sessionId"}, confidence=0.85)
节点: "default-project:POST:/api/orders"
```

---

## 五、知识图谱的学习机制

### 5.1 学习时机

场景用例执行成功后，系统会自动学习依赖关系：

**代码位置**：`main_sqlite.py` 第2949行附近

```python
# 场景用例执行后学习依赖（仅 test_case_id 场景，且不影响主流程）
if _kg and test_case_id:
    try:
        # 从test_cases表加载steps
        cursor.execute("SELECT steps FROM test_cases WHERE id = ?", (test_case_id,))
        row = cursor.fetchone()
        if row:
            steps = json.loads(row["steps"])
            
            # 为每个步骤创建节点
            for step in steps:
                node_id = _kg_node_id(project_id, step)
                _kg.ensure_api_node(
                    node_id,
                    path=step.get("api_path"),
                    method=step.get("api_method"),
                )
            
            # 学习依赖边
            for i, step in enumerate(steps):
                to_node_id = _kg_node_id(project_id, step)
                param_mappings = step.get("param_mappings") or []
                
                for mapping in param_mappings:
                    from_step_idx = mapping.get("from_step") - 1  # step_order从1开始
                    if 0 <= from_step_idx < len(steps):
                        from_step = steps[from_step_idx]
                        from_node_id = _kg_node_id(project_id, from_step)
                        
                        # 构建字段映射
                        field_mapping = {
                            mapping.get("to_field"): mapping.get("from_field")
                        }
                        
                        # 添加依赖边（成功执行，增加置信度）
                        _kg.add_dependency(
                            from_node_id,
                            to_node_id,
                            field_mapping=field_mapping,
                            source_type="execution",
                            source_id=test_case_id,
                            success=True  # 执行成功
                        )
    except Exception as e:
        print(f"知识图谱学习跳过: {e}")
```

### 5.2 置信度更新机制

- **执行成功**：`success_count += 1`，置信度提高
- **执行失败**：`success_count = max(0, success_count - 1)`，置信度降低
- **置信度计算**：`confidence = success_count / count`

这样，**用得越多的依赖关系，置信度越高**；**经常失败的依赖关系，置信度会逐渐降低**，最终被过滤掉。

---

## 六、实际使用示例

### 6.1 场景1：知识图谱有前置依赖

**用户操作**：为 `GET /api/user/profile` 生成单接口测试

**执行流程**：

1. **构建节点ID**：
   ```
   node_id = "default-project:GET:/api/user/profile"
   ```

2. **查询知识图谱**：
   ```python
   preds = _kg.get_predecessors(node_id, min_confidence=0.5, limit=5)
   # 返回：
   # [{
   #     "from_api": "default-project:POST:/api/login",
   #     "path": "/api/login",
   #     "method": "POST",
   #     "field_mapping": {"Authorization": "data.token"},
   #     "confidence": 0.9
   # }]
   ```

3. **构建依赖链**：
   ```python
   chain = [{
       "api_path": "/api/login",
       "api_method": "POST",
       "reason": "知识图谱前置"
   }]
   ```

4. **应用依赖链**：
   ```python
   # 原始steps：
   steps = [{
       "step_order": 1,
       "api_path": "/api/user/profile",
       "api_method": "GET",
       ...
   }]
   
   # 应用后：
   steps = [
       {
           "step_order": 1,
           "api_path": "/api/login",
           "api_method": "POST",
           "description": "[前置依赖] 知识图谱前置",
           "param_mappings": []
       },
       {
           "step_order": 2,
           "api_path": "/api/user/profile",
           "api_method": "GET",
           "param_mappings": [{
               "from_step": 1,
               "from_field": "data.token",
               "to_field": "Authorization",
               "to_type": "headers"
           }]
       }
   ]
   ```

5. **结果**：直接返回，**跳过RAG+AI流程**，减少延迟

### 6.2 场景2：知识图谱无前置依赖

**用户操作**：为 `POST /api/login` 生成单接口测试

**执行流程**：

1. **检查是否为登录类接口**：
   ```python
   login_keywords = ["login", "signin", "auth", "token"]
   if any(k in path.lower() for k in login_keywords):
       return steps  # 直接跳过
   ```

2. **结果**：直接返回原始steps，无需添加前置依赖

### 6.3 场景3：知识图谱查询失败或无结果

**执行流程**：

1. **知识图谱查询**：返回空列表或抛出异常
2. **继续执行RAG检索**：使用关键词检索关联接口
3. **AI分析**：将目标接口和关联接口传给AI，分析是否需要前置依赖
4. **生成依赖链**：AI生成依赖链（如果有）
5. **应用依赖链**：插入前置步骤

---

## 七、优势总结

### 7.1 性能优势

- **减少AI调用**：知识图谱有结果时，直接跳过RAG+AI流程，减少延迟
- **缓存机制**：解析结果缓存，相同接口无需重复解析

### 7.2 准确性优势

- **基于历史数据**：依赖关系来自历史执行结果，更准确
- **置信度过滤**：只使用高置信度（≥0.5）的依赖关系
- **自动学习**：执行即学习，越用越准

### 7.3 可靠性优势

- **容错机制**：知识图谱查询失败不影响主流程，会降级到RAG+AI
- **轻量级实现**：无需额外数据库服务，使用NetworkX+pickle

---

## 八、代码位置总结

| 功能 | 文件 | 行数 |
|------|------|------|
| 知识图谱类定义 | `services/ai-processing/lightweight_services.py` | 20-315 |
| 知识图谱初始化 | `services/ai-processing/main_sqlite.py` | 706-714 |
| 依赖解析函数 | `services/ai-processing/main_sqlite.py` | 165-321 |
| 应用依赖链 | `services/ai-processing/main_sqlite.py` | 324-450 |
| 执行后学习 | `services/ai-processing/main_sqlite.py` | 2949附近 |
| 调用入口 | `services/ai-processing/main_sqlite.py` | 1671, 1790 |

---

## 九、总结

知识图谱在单接口依赖解析中发挥**关键作用**：

1. **优先查询**：在RAG+AI之前，优先查询知识图谱
2. **直接应用**：如果找到高置信度前置依赖，直接构建依赖链，跳过AI分析
3. **性能提升**：减少AI调用，降低延迟
4. **准确性提升**：基于历史执行数据，更准确
5. **自动学习**：执行后自动学习，形成闭环

这种设计实现了**"知识图谱优先，AI兜底"**的策略，既保证了性能，又保证了准确性。
