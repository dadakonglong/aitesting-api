# 场景用例提示词使用说明

## 一、两个提示词的位置

### 提示词1：ScenarioParser的系统提示词

**文件位置**：`services/ai-processing/services/scenario_parser.py` 第51-100行

**使用位置**：`ScenarioParser` 类的 `parse_scenario` 方法中

**调用路径**：
```
main.py
  ↓
初始化 ScenarioParser 实例（第153行）
  ↓
接口：POST /api/v1/ai/parse-scenario（第269行）
  ↓
调用 scenario_parser.parse_scenario()（第276行）
  ↓
使用第一个提示词（scenario_parser.py 第51行）
```

**提示词内容**：
```
你是一个专业的接口测试场景编排专家。
你的任务是根据用户的测试意图和可用的API，生成完整的测试步骤序列。

请生成以下内容：
1. **场景名称** (scenario_name): 简洁的场景名称
2. **场景描述** (description): 详细的场景描述
3. **测试步骤** (steps): 按顺序排列的API调用步骤

每个步骤包含：
- step_order: 步骤序号
- api_id: API标识
- api_name: API名称
- api_path: API路径
- api_method: HTTP方法
- description: 步骤描述
- param_mappings: 参数映射关系（如果需要从前一步获取数据）
```

---

### 提示词2：增强版系统提示词

**文件位置**：`services/ai-processing/main_sqlite.py` 第2411-2423行

**使用位置**：`generate_case` 函数中

**调用路径**：
```
main_sqlite.py
  ↓
接口：POST /api/v1/scenarios/{scenario_id}/generate-case（第2132行）
  ↓
generate_case() 函数（第2133行）
  ↓
AI编排生成步骤（第2409-2433行）
  ↓
使用增强版提示词（第2411行）
```

**提示词内容**：
```
你是个资深自动化专家。任务：根据【业务意图】和【API列表】，生成 JSON 测试步骤。
关键规则：
1. 必须识别依赖：若 A 返回 data.token，B 需使用，则配置 param_mappings。
2. 特别是鉴权：登录返回的 Token 必须映射到后续接口的 Headers，to_field 通常为 "Authorization"，to_type 为 "headers"。
3. 禁止自引用：步骤N不能引用步骤N自己的数据，from_step必须小于当前步骤。
4. 第一步通常无依赖：第一个步骤（通常是登录）的param_mappings应该为空[]。
5. 字段区分：params 放 Body (POST/PUT)，url_params 放 Query String。
6. 真实数据：生成符合逻辑的姓名、手机号等，不要用 {}。
7. 完整覆盖：默认编排完整流程，从 API 列表中选出该流程下的全部相关接口，不要遗漏。
8. 步骤顺序：必须按业务流程的真实先后顺序编排。例如开台(open-pay)→关台(close-room)→清扫(clean-room-finish) 时，清扫必须在关台之后，不能把清扫插在开台和关台之间。先根据业务语义确定正确顺序，再生成 steps。
9. 若下方提供了【知识图谱：常见依赖关系】，优先参考其中的接口先后顺序与字段映射；若无冲突请沿用，若有更优方案可灵活调整。
```

---

## 二、两个提示词的关系

### 使用场景不同

**提示词1（ScenarioParser）**：
- 在 `main.py` 中使用
- 接口：`/api/v1/ai/parse-scenario`
- 可能是**旧版本**或**独立实现**

**提示词2（增强版）**：
- 在 `main_sqlite.py` 中使用
- 接口：`/api/v1/scenarios/{scenario_id}/generate-case`
- 是**实际使用的版本**（更完整、更详细）

### 功能差异

**提示词1的特点**：
- 基础版本
- 输出格式包含 `api_id`、`api_name` 等字段
- 相对简单

**提示词2的特点（增强版）**：
- **更详细的规则**：9条关键规则，覆盖依赖识别、鉴权、字段区分等
- **知识图谱集成**：规则9明确要求参考知识图谱依赖关系
- **更严格的格式要求**：明确禁止返回空steps或reason
- **更智能的依赖识别**：自动识别token、sessionId等依赖
- **业务逻辑考虑**：要求按真实业务流程顺序编排

---

## 三、实际使用情况

### 当前项目主要使用

**增强版提示词**（`main_sqlite.py` 中的）

**原因**：
1. **功能更完整**：包含9条详细规则，覆盖更多场景
2. **知识图谱集成**：明确要求参考知识图谱依赖关系
3. **实际接口**：`/api/v1/scenarios/{scenario_id}/generate-case` 是实际使用的接口
4. **步骤增强**：生成后还有 `_enhance_steps_with_headers` 函数进行增强

### 使用流程

```
用户创建场景
    ↓
POST /api/v1/scenarios（场景理解）
    ↓
用户点击"生成用例"
    ↓
POST /api/v1/scenarios/{scenario_id}/generate-case
    ↓
generate_case() 函数
    ├─ API检索（向量/关键词）
    ├─ 知识图谱增强（查询依赖边）
    ├─ AI编排生成（使用增强版提示词）← 这里使用
    └─ 步骤增强（合并headers、补齐动态头映射）
    ↓
保存到test_cases表
```

---

## 四、增强版提示词的优势

### 1. 知识图谱集成

**规则9**：
```
若下方提供了【知识图谱：常见依赖关系】，优先参考其中的接口先后顺序与字段映射
```

**作用**：
- 明确要求AI参考知识图谱
- 减少AI幻觉
- 提高准确性

### 2. 更详细的依赖识别规则

**规则1-4**：
- 必须识别依赖
- 特别是鉴权（Token映射）
- 禁止自引用
- 第一步通常无依赖

**作用**：
- 确保生成的步骤序列正确
- 避免常见的依赖错误

### 3. 业务逻辑考虑

**规则7-8**：
- 完整覆盖流程
- 按真实业务流程顺序编排

**作用**：
- 生成完整的业务场景
- 确保步骤顺序符合业务逻辑

### 4. 严格的格式要求

**最后一段**：
```
必须返回 steps 数组且至少包含 1 个步骤
禁止返回空 steps 或仅返回 reason
```

**作用**：
- 确保AI返回有效结果
- 减少格式错误

---

## 五、总结

### 提示词1（ScenarioParser）
- **位置**：`scenario_parser.py`
- **使用**：`main.py` 中的 `/api/v1/ai/parse-scenario` 接口
- **特点**：基础版本，相对简单

### 提示词2（增强版）
- **位置**：`main_sqlite.py` 第2411行
- **使用**：`/api/v1/scenarios/{scenario_id}/generate-case` 接口
- **特点**：功能完整，知识图谱集成，实际使用版本

### 建议

**文档中应该重点说明增强版提示词**，因为：
1. 这是实际使用的版本
2. 功能更完整
3. 集成了知识图谱
4. 有详细的规则和格式要求

---

**说明**：增强版提示词在 `main_sqlite.py` 的 `generate_case` 函数中使用，这是场景用例生成的核心接口。
