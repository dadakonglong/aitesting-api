# 知识图谱实现逻辑通俗版

## 一、知识图谱是什么？

**简单理解**：知识图谱就像一个"接口依赖关系记录本"，记录着：
- 哪些接口需要先调用哪个接口（比如获取用户信息前需要先登录）
- 从前一个接口的响应中取什么数据，放到下一个接口的什么地方（比如从登录接口取token，放到获取用户信息的请求头里）

**存储方式**：用一个文件（knowledge_graph.pkl）保存，就像Excel表格一样，但格式是专门用来存图关系的。

---

## 二、什么时候查询知识图谱？

### 场景1：单接口测试执行前

**时机**：用户要测试一个接口（比如"获取用户信息"），点击执行按钮后，**在实际发送请求之前**

**流程**：

```
用户点击"执行"按钮
    ↓
系统检查：这个接口需要先登录吗？
    ↓
【查询知识图谱】
    ↓
知识图谱回答：
  - "需要！这个接口在历史上，有90%的情况都是先调用登录接口"
  - "登录接口返回的token，要放到Authorization请求头里"
    ↓
系统自动插入登录步骤
    ↓
执行：先登录 → 再获取用户信息
```

**如果没有查询到**：
```
查询知识图谱
    ↓
知识图谱回答："没记录，不知道"
    ↓
系统用其他方式（AI分析）判断是否需要前置依赖
```

### 场景2：场景用例生成时

**时机**：用户输入"登录并获取用户信息"，系统生成场景用例时

**流程**：

```
用户输入："登录并获取用户信息"
    ↓
系统找到相关接口：登录接口、获取用户信息接口
    ↓
【查询知识图谱】
    ↓
知识图谱回答：
  - "这两个接口有依赖关系"
  - "登录在前，获取用户信息在后"
  - "token要映射到Authorization"
    ↓
系统把这些信息告诉AI
    ↓
AI参考这些信息，生成正确的步骤顺序和参数映射
```

---

## 三、查询知识图谱的逻辑流程

### 3.1 单接口执行前的查询流程

**步骤1：准备查询条件**

系统需要知道：
- 要测试哪个接口？（比如：GET /api/user/profile）
- 是哪个项目的？（比如：default-project）

**步骤2：构建查询标识**

系统把接口信息组合成一个"身份证号"：
```
项目ID + HTTP方法 + 接口路径
= "default-project:GET:/api/user/profile"
```

这个"身份证号"就是知识图谱里这个接口的唯一标识。

**步骤3：查询前置依赖**

系统问知识图谱：
> "这个接口（default-project:GET:/api/user/profile），在历史上，有哪些接口经常在它之前被调用？"

知识图谱的回答逻辑：
1. 在记录本里找这个接口
2. 看看有哪些接口指向它（就像看谁在它前面排队）
3. 检查每个前置接口的"可信度"（比如90%的情况都成功，可信度就是0.9）
4. 只返回可信度≥50%的前置接口
5. 按可信度从高到低排序，最多返回5个

**步骤4：判断是否有结果**

**情况A：查询到前置依赖**

知识图谱返回：
```
前置接口1：POST /api/login
  - 可信度：90%
  - 字段映射：Authorization ← data.token（从登录响应中取token，放到Authorization请求头）
```

系统操作：
1. 把这个前置接口插入到执行步骤的最前面
2. 配置好参数映射（token怎么传递）
3. 直接执行，**不再问AI**（节省时间）

**情况B：没查询到**

知识图谱返回：空（没有记录）

系统操作：
1. 用其他方式判断（比如AI分析、关键词匹配）
2. 如果AI判断需要前置依赖，再插入

---

## 四、知识图谱是怎么学习的？

### 4.1 学习时机

**什么时候学习？**

场景用例执行完成后，如果执行成功，系统会自动学习。

**为什么只学成功的？**

因为成功的执行说明依赖关系是对的，失败的可能是其他原因（比如接口本身有问题），不能说明依赖关系不对。

### 4.2 学习流程

**场景**：用户执行了一个场景用例"登录并获取用户信息"

**执行步骤**：
```
步骤1：POST /api/login（登录）
步骤2：GET /api/user/profile（获取用户信息）
  - 参数映射：从步骤1的响应中取 data.token，放到步骤2的 Authorization 请求头
```

**执行结果**：成功 ✅

**学习过程**：

**第1步：记录接口信息**

系统把两个接口都记录到知识图谱：
```
接口1：default-project:POST:/api/login
接口2：default-project:GET:/api/user/profile
```

**第2步：记录依赖关系**

系统发现步骤2有参数映射，说明步骤2依赖步骤1：
```
从步骤1 → 步骤2
字段映射：Authorization ← data.token
```

系统在知识图谱里创建一条"依赖关系记录"：
```
依赖关系：
  从：default-project:POST:/api/login
  到：default-project:GET:/api/user/profile
  字段映射：{"Authorization": "data.token"}
  执行次数：1次
  成功次数：1次
  可信度：100%（1次成功 / 1次执行）
```

**第3步：更新可信度**

如果这个依赖关系之前就存在，系统会更新：
```
之前：执行了10次，成功了9次，可信度90%
现在：执行了11次，成功了10次，可信度91%（提高）
```

如果执行失败，可信度会降低：
```
之前：执行了10次，成功了9次，可信度90%
现在：执行了11次，成功了8次，可信度73%（降低）
```

**第4步：保存到文件**

系统把更新后的知识图谱保存到文件（knowledge_graph.pkl），下次启动时还能用。

---

## 五、知识图谱的数据结构（通俗理解）

### 5.1 接口节点（就像通讯录里的联系人）

**每个接口就是一个节点**，包含：
- **身份证号**（节点ID）：`项目ID:HTTP方法:接口路径`
  - 例如：`default-project:POST:/api/login`
- **基本信息**：
  - 接口路径：`/api/login`
  - HTTP方法：`POST`
  - 接口名称：`用户登录`（可选）

### 5.2 依赖关系（就像联系人之间的关系）

**每条依赖关系就是一条"连线"**，包含：
- **从哪个接口**：`default-project:POST:/api/login`
- **到哪个接口**：`default-project:GET:/api/user/profile`
- **怎么传递数据**（字段映射）：
  ```
  Authorization ← data.token
  （从登录接口的响应中取data.token，放到获取用户信息的Authorization请求头）
  ```
- **可信度**：
  - 执行了10次，成功了9次
  - 可信度 = 9/10 = 90%
- **统计信息**：
  - 总执行次数：10次
  - 成功次数：9次
  - 最后使用时间：2024-01-15

### 5.3 图结构（就像关系网）

```
登录接口（节点）
    │
    │ 依赖关系（边）
    │ - 字段映射：Authorization ← data.token
    │ - 可信度：90%
    │
    ▼
获取用户信息接口（节点）
    │
    │ 依赖关系（边）
    │ - 字段映射：sessionId ← data.sessionId
    │ - 可信度：85%
    │
    ▼
创建订单接口（节点）
```

---

## 六、完整的数据流转过程

### 6.1 第一次使用（知识图谱是空的）

```
用户执行场景用例："登录并获取用户信息"
    ↓
执行成功 ✅
    ↓
系统学习：
  - 记录登录接口
  - 记录获取用户信息接口
  - 记录依赖关系：登录 → 获取用户信息
  - 可信度：100%（1次成功/1次执行）
    ↓
保存到知识图谱文件
```

### 6.2 第二次使用（知识图谱有记录了）

```
用户要测试"获取用户信息"接口
    ↓
系统在执行前查询知识图谱：
  "获取用户信息接口需要前置依赖吗？"
    ↓
知识图谱回答：
  "需要！需要先调用登录接口，可信度100%"
    ↓
系统自动插入登录步骤
    ↓
执行：先登录 → 再获取用户信息
    ↓
执行成功 ✅
    ↓
系统更新知识图谱：
  - 依赖关系：登录 → 获取用户信息
  - 执行次数：2次
  - 成功次数：2次
  - 可信度：100%（2次成功/2次执行）
```

### 6.3 多次使用后（知识图谱越来越准确）

```
第10次执行场景用例："登录并获取用户信息"
    ↓
执行成功 ✅
    ↓
系统更新知识图谱：
  - 执行次数：10次
  - 成功次数：9次（假设有1次失败）
  - 可信度：90%（9次成功/10次执行）
    ↓
第11次，用户要测试"获取用户信息"接口
    ↓
系统查询知识图谱
    ↓
知识图谱回答：
  "需要先调用登录接口，可信度90%"
    ↓
系统使用这个依赖关系（因为90% > 50%阈值）
```

---

## 七、为什么这样设计？

### 7.1 为什么在执行前查询？

**目的**：减少AI调用，提高速度

**对比**：

**不用知识图谱**：
```
用户要测试接口
    ↓
问AI："这个接口需要前置依赖吗？"（需要调用AI，慢）
    ↓
AI回答："需要登录"
    ↓
插入登录步骤
    ↓
执行
```

**用知识图谱**：
```
用户要测试接口
    ↓
查知识图谱："这个接口需要前置依赖吗？"（查文件，快）
    ↓
知识图谱回答："需要登录，可信度90%"
    ↓
直接插入登录步骤（不再问AI）
    ↓
执行
```

**优势**：
- 速度快：查文件比问AI快得多
- 更准确：基于历史真实数据，不是AI猜测
- 省钱：减少AI调用次数

### 7.2 为什么要有可信度？

**目的**：过滤掉不靠谱的依赖关系

**场景**：
- 某个依赖关系执行了10次，只成功了3次，可信度30%
- 这个依赖关系可能不对，或者有其他问题
- 系统不会使用它（因为30% < 50%阈值）

**好处**：
- 自动淘汰低质量的依赖关系
- 只使用经过验证的、可靠的依赖关系

### 7.3 为什么执行失败会降低可信度？

**目的**：让系统自动修正错误的依赖关系

**场景**：
- 某个依赖关系之前可信度很高（90%）
- 但最近几次执行都失败了
- 可信度逐渐降低（90% → 80% → 70% → ...）
- 当可信度低于50%时，系统不再使用它

**好处**：
- 系统会自动"忘记"错误的依赖关系
- 不需要人工手动删除

---

## 八、实际使用示例

### 示例1：第一次执行场景用例

**用户操作**：创建场景用例"登录并获取用户信息"，执行

**系统操作**：
1. 执行步骤1：登录接口
2. 执行步骤2：获取用户信息接口（使用步骤1返回的token）
3. 执行成功 ✅
4. **学习到知识图谱**：
   - 记录：登录接口 → 获取用户信息接口
   - 字段映射：Authorization ← data.token
   - 可信度：100%

### 示例2：第二次测试单接口

**用户操作**：直接测试"获取用户信息"接口

**系统操作**：
1. **执行前查询知识图谱**：
   - 问："获取用户信息接口需要前置依赖吗？"
   - 答："需要，需要先调用登录接口，可信度100%"
2. **自动插入登录步骤**：
   - 步骤1：登录（系统自动添加）
   - 步骤2：获取用户信息（用户要测试的）
3. 执行两个步骤
4. 执行成功 ✅
5. **更新知识图谱**：
   - 依赖关系：登录 → 获取用户信息
   - 执行次数：2次
   - 成功次数：2次
   - 可信度：100%

### 示例3：多次使用后

**用户操作**：第10次执行场景用例"登录并获取用户信息"

**系统操作**：
1. 执行成功 ✅
2. **更新知识图谱**：
   - 执行次数：10次
   - 成功次数：9次（假设有1次失败）
   - 可信度：90%

**下次查询时**：
- 系统查询："获取用户信息接口需要前置依赖吗？"
- 知识图谱回答："需要，需要先调用登录接口，可信度90%"
- 系统使用这个依赖关系（因为90% > 50%）

---

## 九、总结

### 9.1 核心逻辑

1. **查询时机**：单接口执行前、场景生成时
2. **查询方式**：用接口的"身份证号"查前置依赖
3. **使用条件**：可信度≥50%才使用
4. **学习时机**：场景用例执行成功后自动学习
5. **更新方式**：成功提高可信度，失败降低可信度

### 9.2 优势

1. **速度快**：查文件比问AI快
2. **更准确**：基于历史真实数据
3. **自动学习**：越用越准
4. **自动修正**：低可信度的依赖关系自动淘汰

### 9.3 数据流转闭环

```
场景执行 → 学习依赖 → 保存到文件 → 查询依赖 → 使用依赖 → 执行测试
   ↑                                                          │
   └──────────────────────────────────────────────────────────┘
                    (形成闭环，越用越准)
```

---

## 十、常见问题

### Q1: 知识图谱什么时候会变空？

**A**: 
- 第一次使用时，知识图谱是空的
- 删除知识图谱文件后，会创建新的空图谱
- 但系统会逐渐学习，慢慢积累

### Q2: 如果知识图谱记录错了怎么办？

**A**: 
- 如果这个依赖关系经常失败，可信度会逐渐降低
- 当可信度低于50%时，系统不再使用它
- 也可以手动删除错误的依赖关系

### Q3: 知识图谱会影响执行速度吗？

**A**: 
- 不会，反而会提高速度
- 查文件比问AI快得多
- 如果查询到依赖关系，可以跳过AI分析，直接执行

### Q4: 为什么有些接口查不到前置依赖？

**A**: 
- 可能是第一次使用，知识图谱还没有学习到
- 可能是这个接口确实不需要前置依赖（比如登录接口本身）
- 系统会用其他方式（AI分析）判断

### Q5: 知识图谱会越来越大吗？

**A**: 
- 会，但随着使用会逐渐稳定
- 通常一个项目的接口数量是有限的（几十到几百个）
- 文件大小通常在几KB到几MB之间，完全不用担心

---

**文档说明**：本文档用通俗语言描述知识图谱的实现逻辑，不涉及具体代码实现细节。
