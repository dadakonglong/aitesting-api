# AI接口测试平台工作逻辑说明文档

## 第一部分：接口测试用例生成

### 一、整体架构

接口测试用例生成采用**五阶段流水线架构**，由**主编排器（Orchestrator）**统一调度，通过**5个专业智能体（Agent）**协作完成从需求理解到结果分析的全流程。

---

### 二、主编排器（Orchestrator Agent）

#### 2.1 职责与工作

主编排器是整个系统的**核心调度中心**，负责：

1. **意图识别**：分析用户的自然语言请求，识别核心意图类型
   - `generate_test`: 生成测试用例
   - `analyze_api`: 分析接口文档
   - `execute_test`: 执行测试
   - `fix_test`: 修复失败的测试
   - `generate_report`: 生成测试报告

2. **任务拆解**：将用户请求拆解为可执行的子任务
   - 确定每个任务负责的智能体类型
   - 定义具体动作和输入参数
   - 识别任务间的依赖关系

3. **智能体调度**：根据任务类型调度相应的专业智能体
   - 管理5个可用智能体：analyst（分析专家）、planner（策划专家）、executor（执行专家）、healer（自愈专家）、reporter（报告专家）
   - 协调各智能体的执行顺序
   - 汇总执行结果

4. **结果汇总**：生成最终的执行摘要

#### 2.2 主编排器 Prompt

主编排器使用以下系统提示词进行意图识别：

```
你是一个智能测试助手的意图识别模块。
分析用户请求,识别其核心意图。

可能的意图类型:
- generate_test: 生成测试用例
- analyze_api: 分析接口文档
- execute_test: 执行测试
- fix_test: 修复失败的测试
- generate_report: 生成测试报告

返回JSON格式: {"intent_type": "类型", "confidence": 0.9, "entities": [...]}
```

任务拆解提示词：

```
你是任务拆解专家。
根据用户请求和识别的意图,将其拆解为可执行的子任务。

每个任务包含:
- agent: 负责的智能体类型
- action: 具体动作
- inputs: 输入参数
- dependencies: 依赖的前序任务

返回JSON格式的任务列表。
```

---

### 三、五阶段智能体详解

#### 阶段1：需求理解（RAG检索Agent）

**职责**：
- 解析用户的自然语言描述
- 从知识库中检索匹配的API接口
- 提取实体、关系和文本块
- 识别用户要测试的目标接口（单接口或多接口）

**工作流程**：
1. **优先向量语义检索**：如果配置了向量服务（Qdrant），使用语义搜索召回相关API
2. **关键词RAG检索**：向量检索失败或无结果时，使用关键词增强检索（支持6种模式：local、global、hybrid、naive、mix、bypass）
3. **大模型结构化分析**：将检索结果传给大模型，提取entities、relationships、chunks、target_api_indices

**Prompt（RAG_RETRIEVAL_SYSTEM_PROMPT）**：
```
你是接口分析专家。你的任务是对用户要测试的接口做结构化分析。

## 分析范围
- **单接口**：若用户描述仅针对一个接口（如「为登录接口生成测试」），分析该接口。
- **多接口**：若用户描述明确提到多个接口（如「登录、注册、忘记密码」「用户和订单接口」），需识别所有目标接口并分析。
- 根据用户描述判断是 1 个还是多个接口，通过 target_api_indices 指定检索结果中的目标索引。

## 输出格式
请以 JSON 格式返回，包含：
- entities：实体列表（每项含 entity_name, entity_type, description），针对目标接口。
- relationships：关系列表。
- chunks：文本块列表（每项含 content），针对目标接口的请求参数、请求体、响应格式、认证方式等。
- target_api_indices：整数数组，表示用户要测试的接口在检索结果中的索引（0-based）。例如 [0] 表示只测第 1 个接口；[0,1,2] 表示测前 3 个接口。根据用户描述判断：若明确提到多个接口（如「登录、注册、忘记密码」「这三个接口」），则返回对应索引；否则默认 [0]。
```

**知识图谱使用**：
- 本阶段**不使用**知识图谱，主要依赖向量检索和关键词匹配

---

#### 阶段2：测试计划（Planner Agent）

**职责**：
- 根据结构化API信息制定测试计划
- 生成Markdown格式的测试计划文档
- 同时调用ApiPlanner + ai_generate_cases生成可执行用例列表

**工作流程**：
1. 接收阶段1的结构化信息（entities、chunks、api_candidates、target_api_indices）
2. 确定要测试的目标接口列表（根据target_api_indices）
3. 调用Planner Agent生成Markdown测试计划
4. 为每个目标接口调用AI用例生成器，生成4类用例：positive（正向）、boundary（边界）、robustness（健壮）、security（安全）

**Prompt（PLANNER_SYSTEM_PROMPT）**：
```
你是测试计划专家。你的任务是为目标接口制定测试计划。

## 范围
- 若为单接口，仅针对该接口制定计划。
- 若为多接口，需覆盖所有目标接口的测试内容。

## 测试类型
- **功能测试**：验证该接口功能正确性
- **安全测试**：验证认证、授权、输入验证
- **边界测试**：验证边界条件和异常情况
- **健壮性测试**：缺参、类型错误、格式错误等

## 输出格式
生成 Markdown 格式的测试计划，包含：测试目标与范围（仅当前接口）、测试用例列表、测试数据与环境要求、预期结果与验收标准。

请以 JSON 格式返回：{"markdown": "完整 Markdown 字符串"}。
```

**知识图谱使用**：
- 本阶段**不使用**知识图谱，主要基于API定义和测试策略生成计划

---

#### 阶段3：代码生成（Generator Agent）

**职责**：
- 根据测试计划和用例列表生成可执行的测试代码
- 支持两种框架：Playwright（TypeScript）和pytest（Python）

**工作流程**：
1. 优先使用测试计划+用例列表生成Playwright代码
2. 若无plan_payload，则退回生成pytest代码
3. 从大模型响应中提取代码（支持JSON格式和markdown代码块）

**Prompt（GENERATOR_PLAYWRIGHT_SYSTEM_PROMPT）**：
```
你是 Playwright 接口测试代码生成专家。你的任务是根据「测试计划」和「接口用例列表」生成可直接运行的 Playwright 接口测试文件。

## 代码生成的质量要求（非常重要）

1. **严格区分 Headers**：如果用例列表中包含了 `headers`（尤其是安全测试中的 Authorization），**必须**在 `request` 调用中显式配置它们。不要忽略 headers 差异。
2. **增强断言逻辑**：不要只检查 `status().toBe(200)`。
    - **正向用例**：必须先解析 JSON (`const body = await response.json()`)，然后检查关键业务字段（如 `expect(body).toHaveProperty('token')`）。
    - **异常用例**：检查返回的错误消息 `message` 或 `code`。
3. **理解业务逻辑**：登录接口通常不需要 Authorization，资源操作通常需要。
4. **代码风格**：使用 TypeScript，代码整洁。

## 输出格式（必须严格遵循）

- 仅输出一个完整的 TypeScript 测试文件代码。
- 必须包含：import { test, expect } from '@playwright/test';
- **单接口**：使用一个 test.describe() 包裹该接口的所有 test()。
- **多接口**：使用多个 test.describe()，每个接口一个 describe 块，每个用例对应一个 test()。
- 不要解释，直接输出代码。
```

**Prompt（GENERATOR_SYSTEM_PROMPT - pytest版本）**：
```
你是 pytest 测试代码生成专家。
你的任务是根据测试计划生成高质量的 pytest 测试脚本。

## 代码规范
- 使用 pytest + requests 框架
- 集成 Allure 报告装饰器
- 支持参数化测试
- 包含详细的步骤和断言

## 文件结构
生成以下文件：
- test_*.py：测试文件
- conftest.py：pytest 配置和 fixtures
- pytest.ini：pytest 配置文件

请以 JSON 格式返回：{"code": "test_*.py 的完整代码", "conftest": "conftest.py 内容（可选）", "pytest_ini": "pytest.ini 内容（可选）"}。至少包含 code。不要用 markdown 代码块包裹。
```

**知识图谱使用**：
- 本阶段**不使用**知识图谱，主要基于测试计划和用例列表生成代码

---

#### 阶段4：测试执行（Executor Agent）

**职责**：
- **依赖解析**：在执行前优先查询知识图谱，检查是否有前置依赖
- **实际执行**：由后端执行引擎执行所有步骤（包括前置依赖）

**工作流程**：
1. **依赖解析（子步骤1）**：
   - 检查缓存（如果该接口的依赖已解析过，直接使用）
   - 跳过登录类接口（登录接口本身不需要前置依赖）
   - **★ 优先查询知识图谱**：调用 `_kg.get_predecessors()` 查询前置依赖
     - 如果找到高置信度（≥0.5）的前置依赖，直接构建依赖链并返回（跳过RAG+AI）
   - RAG检索（知识图谱无结果时）：使用关键词检索关联接口
   - AI分析（RAG有结果时）：将目标接口和关联接口传给AI，分析是否需要前置依赖
   - 应用依赖链：将依赖链转换为真实的执行步骤，插入到原始steps之前

2. **实际执行（子步骤2）**：
   - 执行所有步骤（包括知识图谱插入的前置步骤）
   - 收集执行结果

**代码位置**：
- 依赖解析：`main_sqlite.py` 第1671-1674行
- 实际执行：`main_sqlite.py` 第1677行

**Prompt（EXECUTOR_SYSTEM_PROMPT）**：
```
你是测试执行专家。
你的任务是执行 pytest 测试并收集结果。

## 执行策略
- 支持 pytest-xdist 并行执行
- 自动生成 Allure JSON 结果
- 可生成 Allure HTML 报告
- 支持失败重试

## 结果收集
收集以下信息：
- 测试通过/失败/跳过数量
- 执行时间和性能数据
- 失败用例的详细日志
- Allure 报告路径

当前由后端执行 HTTP 用例（非本地 pytest），请根据测试计划与用例列表，输出 JSON：{"execution_summary": "执行策略简要说明", "cases_to_run": "将执行的用例数量或说明"}。
```

**知识图谱使用**：
- **本阶段使用知识图谱**：在执行前的依赖解析子步骤中，优先查询知识图谱
- 如果知识图谱有高置信度前置依赖，直接应用，跳过RAG+AI流程
- 如果知识图谱无结果或查询失败，降级到RAG+AI流程

---

#### 阶段5：结果分析（Analyzer Agent）

**职责**：
- 分析测试执行结果
- 生成Markdown格式的分析报告
- 提供测试摘要、失败分析、根因识别、优化建议

**工作流程**：
1. 接收测试执行结果（total_cases、passed_cases、failed_cases、duration_ms、case_results）
2. 调用Analyzer Agent生成分析报告
3. 生成图表数据（可选）

**Prompt（ANALYZER_SYSTEM_PROMPT）**：
```
你是测试结果分析专家。
你的任务是分析测试结果并提供洞察。

## 分析内容
- 执行概览和统计
- 失败用例分析
- 根因识别
- 改进建议
- **报告生成时间**: 必须在报告开头使用用户提供的 current_time，格式为 "*报告生成时间: YYYY-MM-DD HH:MM:SS*"
- **禁止生成一级标题**: 报告不要包含 "# 标题", 因为系统会自动添加。可以直接从 ## 二级标题开始。
- **禁止生成图表**: 严禁包含 mermaid、gantt、pie 等可视化图表代码块。
- **禁止包含章节**: 严禁包含 "可视化图表"、"单接口" 等章节标题。
- **报告生成时间**: 必须在报告开头使用我传给你的 json 数据中的 `current_time`，格式为 "*报告生成时间: {current_time}*"。严禁编造时间。

## 报告格式
生成 Markdown 格式的分析报告，确保：
1. 不要使用 # 一级标题。
2. 不要包含"可视化图表"章节。
3. 严格使用提供的 current_time。
4. 包含：测试摘要、失败分析、根因识别、优化建议。

请以 JSON 格式返回：{"report": "完整 Markdown 报告字符串"}。
```

**知识图谱使用**：
- 本阶段**不使用**知识图谱，主要基于执行结果进行分析

---

### 四、知识图谱在接口用例生成中的使用

#### 4.1 使用阶段总结

在**接口测试用例生成的五阶段流程中，知识图谱在阶段4（测试执行）中使用**：

**阶段4（测试执行）包含两个子步骤**：

1. **依赖解析（执行前）**：
   - 在阶段4的实际执行步骤之前，系统会**优先查询知识图谱**，检查该接口是否有前置依赖（如登录接口）
   - 如果有高置信度（≥0.5）的前置依赖，会直接插入前置步骤，避免走RAG+AI流程，减少延迟和错误
   - **代码位置**：`main_sqlite.py` 第1671-1674行，在 `_run_steps()` 之前调用

2. **实际执行**：
   - 执行所有步骤（包括知识图谱插入的前置步骤）

#### 4.2 知识图谱在阶段4中的使用流程

```
阶段4：测试执行
    ↓
【子步骤1：依赖解析】
    ├─ 检查缓存
    ├─ 跳过登录类接口
    ├─ ★ 知识图谱查询（优先）
    │   └─ 如果找到高置信度前置依赖 → 直接构建依赖链 → 返回（跳过RAG+AI）
    ├─ RAG检索（知识图谱无结果时）
    └─ AI分析（RAG有结果时）
    ↓
【子步骤2：实际执行】
    └─ 执行所有步骤（包括前置依赖）
```

#### 4.3 知识图谱的作用

知识图谱在阶段4中发挥关键作用：

- **优先查询**：在执行前优先查询知识图谱，减少AI调用
- **依赖关系记录**：记录接口间的调用顺序和依赖关系
- **参数映射学习**：记录从前一步响应中提取字段并映射到下一步的逻辑
- **执行前补全**：自动插入必要的登录等前置步骤
- **执行后学习**：场景用例执行成功后，会将新的依赖关系学习到知识图谱中（形成闭环）

---

### 五、接口用例生成完整流程

```
用户输入自然语言描述
    ↓
【阶段1：需求理解】RAG检索Agent
    - 向量检索/关键词检索API
    - 大模型结构化分析
    - 输出：entities, chunks, api_candidates, target_api_indices
    ↓
【阶段2：测试计划】Planner Agent
    - 生成Markdown测试计划
    - 调用AI用例生成器生成4类用例
    - 输出：plan_markdown, endpoints[].cases
    ↓
【阶段3：代码生成】Generator Agent
    - 根据测试计划+用例列表生成Playwright/pytest代码
    - 输出：可执行的测试代码
    ↓
【阶段4：测试执行】Executor Agent
    ├─ 子步骤1：依赖解析
    │   ├─ ★ 优先查询知识图谱（如果有前置依赖则直接应用）
    │   ├─ RAG检索（知识图谱无结果时）
    │   └─ AI分析（RAG有结果时）
    └─ 子步骤2：实际执行
        └─ 执行所有步骤（包括前置依赖）
    - 输出：执行结果
    ↓
【阶段5：结果分析】Analyzer Agent
    - 分析执行结果
    - 生成Markdown报告
    - 输出：分析报告
```

---

### 六、关键特性

1. **多接口支持**：通过target_api_indices支持单接口和多接口测试
2. **用例类型覆盖**：自动生成正向、边界、健壮、安全四类用例
3. **代码框架灵活**：支持Playwright（TypeScript）和pytest（Python）
4. **分层断言**：遵循分层断言原则，避免过度断言
5. **知识图谱增强**：执行前依赖解析优先使用知识图谱，减少AI幻觉

---

## 总结

接口测试用例生成采用**五阶段流水线**架构，主编排器负责统一调度，5个专业智能体各司其职。**知识图谱在阶段4（测试执行）的依赖解析子步骤中发挥关键作用**，优先查询知识图谱，如果有高置信度前置依赖则直接应用，减少AI调用和延迟。执行后自动学习新的依赖关系，形成"越用越准"的闭环。
