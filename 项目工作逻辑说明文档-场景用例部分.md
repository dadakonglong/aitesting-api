# AI接口测试平台工作逻辑说明文档

## 第二部分：场景用例生成

### 一、整体架构

场景用例生成采用**四阶段架构**：
1. **场景理解**：通过**场景理解Agent（NLU）**解析自然语言描述
2. **场景编排**：通过**场景解析器（ScenarioParser）**生成完整测试步骤序列
3. **测试执行与结果分析**：执行测试步骤，分析业务状态码，判断是否通过
4. **自愈修复**：通过**自愈专家Agent（Healer Agent）**分析失败原因并自动修复

从自然语言描述到最终通过测试，形成完整的自动化测试闭环。

---

### 二、阶段1：场景理解（NLU Agent）

#### 2.1 职责与工作

场景理解Agent负责：
1. **解析用户描述**：理解用户的自然语言测试场景描述
2. **提取结构化信息**：
   - `intent`：测试意图
   - `entities`：涉及的实体
   - `actions`：动作序列
   - `expected_results`：预期结果
3. **保存场景信息**：将理解结果保存到数据库

#### 2.2 工作流程

```
用户输入："为登录并获取用户信息接口生成完整的场景测试"
    ↓
场景理解Agent（NLU）
    ↓
输出JSON：{
  "intent": "登录并查看个人资料",
  "entities": ["登录接口", "用户信息接口"],
  "actions": ["登录", "获取用户信息"],
  "expected_results": "成功获取用户资料"
}
    ↓
保存到scenarios表
```

#### 2.3 Prompt

**系统提示词**：
```
你是一个接口测试专家。请解析用户描述的测试场景，提取意图、涉及实体和动作序列。以 JSON 格式返回：{intent, entities, actions, expected_results}
```

**知识图谱使用**：
- 本阶段**不使用**知识图谱，主要进行自然语言理解

---

### 三、阶段2：场景编排（ScenarioParser）

#### 3.1 职责与工作

场景解析器（ScenarioParser）是场景用例生成的核心组件，负责：

1. **API检索**：
   - 优先使用向量语义检索（Qdrant）从项目API库中召回相关接口
   - 无向量服务时，使用关键词排序和同义词扩展
   - 支持同义词扩展（如"点单流程" → "点单 点单立结 order"）

2. **知识图谱增强**：
   - 从知识图谱中获取与候选API相关的高置信度依赖边
   - 将依赖关系以"知识图谱：常见依赖关系"的形式注入到Prompt中
   - 提供接口间的调用顺序和字段映射参考

3. **AI编排生成**：
   - 根据业务意图、可用API列表和知识图谱依赖关系
   - 生成完整的测试步骤序列（steps）
   - 自动识别参数依赖和鉴权映射

4. **步骤增强**：
   - 自动合并API headers
   - 补齐动态头映射（如X-Employee-Id、X-Venue-Id等）
   - 注入API Schema（params_schema、body_schema）

#### 3.2 工作流程

```
场景理解结果（NLU）
    ↓
【API检索】
- 向量语义检索（优先）
- 关键词排序（备选）
- 同义词扩展
    ↓
【知识图谱增强】
- 查询与候选API相关的依赖边
- 构建"知识图谱：常见依赖关系"上下文
    ↓
【AI编排生成】
- 输入：意图 + API列表 + 知识图谱依赖
- 输出：JSON格式的测试步骤序列
    ↓
【步骤增强】
- 合并headers
- 补齐动态头映射
- 注入API Schema
    ↓
保存到test_cases表
```

#### 3.3 Prompt

**系统提示词（ScenarioParser）**：
```
你是一个专业的接口测试场景编排专家。
你的任务是根据用户的测试意图和可用的API，生成完整的测试步骤序列。

请生成以下内容：
1. **场景名称** (scenario_name): 简洁的场景名称
2. **场景描述** (description): 详细的场景描述
3. **测试步骤** (steps): 按顺序排列的API调用步骤

每个步骤包含：
- step_order: 步骤序号
- api_id: API标识
- api_name: API名称
- api_path: API路径
- api_method: HTTP方法
- description: 步骤描述
- param_mappings: 参数映射关系（如果需要从前一步获取数据）

以JSON格式返回：
{
  "scenario_name": "场景名称",
  "description": "场景描述",
  "steps": [
    {
      "step_order": 1,
      "api_id": "POST:/api/login",
      "api_name": "用户登录",
      "api_path": "/api/login",
      "api_method": "POST",
      "description": "用户登录获取token",
      "param_mappings": []
    },
    {
      "step_order": 2,
      "api_id": "POST:/api/orders",
      "api_name": "创建订单",
      "api_path": "/api/orders",
      "api_method": "POST",
      "description": "创建订单",
      "param_mappings": [
        {
          "from_step": 1,
          "from_field": "response.token",
          "to_field": "headers.Authorization"
        }
      ]
    }
  ],
  "confidence": 0.9
}
```

**增强版系统提示词（main_sqlite.py中的场景生成）**：
```
你是个资深自动化专家。任务：根据【业务意图】和【API列表】，生成 JSON 测试步骤。
关键规则：
1. 必须识别依赖：若 A 返回 data.token，B 需使用，则配置 param_mappings。
2. 特别是鉴权：登录返回的 Token 必须映射到后续接口的 Headers，to_field 通常为 "Authorization"，to_type 为 "headers"。
3. 禁止自引用：步骤N不能引用步骤N自己的数据，from_step必须小于当前步骤。
4. 第一步通常无依赖：第一个步骤（通常是登录）的param_mappings应该为空[]。
5. 字段区分：params 放 Body (POST/PUT)，url_params 放 Query String。
6. 真实数据：生成符合逻辑的姓名、手机号等，不要用 {}。
7. 完整覆盖：默认编排完整流程，从 API 列表中选出该流程下的全部相关接口，不要遗漏。
8. 步骤顺序：必须按业务流程的真实先后顺序编排。例如开台(open-pay)→关台(close-room)→清扫(clean-room-finish) 时，清扫必须在关台之后，不能把清扫插在开台和关台之间。先根据业务语义确定正确顺序，再生成 steps。
9. 若下方提供了【知识图谱：常见依赖关系】，优先参考其中的接口先后顺序与字段映射；若无冲突请沿用，若有更优方案可灵活调整。
格式：{ "scenario_name": "...", "steps": [{ "step_order": 1, "api_path": "...", "api_method": "...", "params": {}, "url_params": {}, "headers": {}, "param_mappings": [{ "from_step": 1, "from_field": "data.token", "to_field": "Authorization", "to_type": "headers" }] }] }
请只输出上述 JSON 对象，不要输出其他说明或键（如 reason、message 等）。必须返回 steps 数组且至少包含 1 个步骤；若无法完整编排也请从可用 API 中选用最相关的 1 个生成 1 步。禁止返回空 steps 或仅返回 reason。
```

#### 3.4 知识图谱使用详解

**使用阶段**：场景编排阶段（阶段2）

**使用方式**：

1. **依赖边查询**：
   ```python
   kg_edges = _kg.get_edges_for_prompt(
       project_id_str, 
       apis_for_prompt, 
       min_confidence=0.5, 
       limit=40
   )
   ```

2. **上下文注入**：
   - 将知识图谱查询到的依赖边转换为可读格式
   - 构建"知识图谱：常见依赖关系"章节
   - 追加到用户Prompt中，供大模型参考

3. **依赖关系格式**：
   ```
   【知识图谱：常见依赖关系（可参考，非强制）】
     POST /api/login → GET /api/user/profile  映射: Authorization <- data.token  (置信度 0.85)
     GET /api/rooms → POST /api/orders  映射: roomId <- data.roomId  (置信度 0.72)
   ```

4. **Prompt中的使用**：
   - 规则9明确要求：若提供了知识图谱依赖关系，优先参考其中的接口先后顺序与字段映射
   - 大模型会根据知识图谱提供的依赖关系，结合业务逻辑生成更准确的步骤序列

**知识图谱的作用**：

1. **提供依赖参考**：告诉大模型哪些接口有依赖关系，以及字段如何映射
2. **减少AI幻觉**：通过历史验证过的依赖关系，减少大模型生成错误的依赖
3. **提高准确性**：优先使用高置信度的依赖关系，提高场景编排的准确性
4. **学习闭环**：场景执行成功后，会将新的依赖关系学习到知识图谱中，形成"越用越准"的闭环

---

### 三、阶段3：测试执行与结果分析

#### 3.1 职责与工作

**执行调度员 (Executor Agent) + 结果分析专家 (Analyzer Agent)**

**职责**：
1. **测试执行**：
   - 按顺序执行场景用例的所有测试步骤
   - 处理参数映射（从前一步提取数据并注入到下一步）
   - 收集每个步骤的执行结果（状态码、响应数据、延迟等）

2. **规则分析**：
   - 分析每个步骤的执行结果
   - 判断业务状态码（code字段）是否为0
   - 提取失败原因（message字段等）
   - 生成规则分析报告

3. **AI智能分析**：
   - 使用AI对执行结果进行深度分析
   - 提供执行概览、失败步骤分析、成功步骤评估
   - 评估业务流程完整性
   - 提供改进建议

**判断规则**：
- **通过条件**：所有步骤的业务状态码（`response.data.code`）都返回为0
- **失败条件**：任意步骤的业务状态码不为0，或HTTP状态码不在2xx范围

#### 3.2 工作流程

```
场景用例（test_cases表）
    ↓
【测试执行】
按步骤顺序执行
    ├─ 处理参数映射（从前一步提取数据）
    ├─ 发送HTTP请求
    └─ 收集响应数据（状态码、响应体、延迟等）
    ↓
保存执行结果到executions表
    ↓
【规则分析】
执行结果
    ↓
分析每个步骤
    ├─ 检查HTTP状态码（是否为2xx）
    ├─ 检查业务状态码（code字段是否为0）
    ├─ 提取错误信息（message字段）
    └─ 生成规则分析报告
    ↓
判断整体状态
    ├─ 所有步骤code=0 → 通过
    └─ 任意步骤code≠0 → 失败
    ↓
【AI智能分析】
执行结果 + 规则分析结果
    ↓
AI分析（场景测试结果分析专家）
    ├─ 执行概览：总结整体执行情况
    ├─ 失败步骤分析：深入分析失败原因
    │   ├─ HTTP状态码分析
    │   ├─ 业务状态码分析
    │   ├─ 错误信息解读
    │   └─ 可能的根因
    ├─ 成功步骤评估：评估成功步骤是否真正符合预期
    ├─ 业务流程完整性：评估整个业务流程是否完整执行
    └─ 改进建议：提供具体的优化建议
    ↓
生成AI分析报告
    ↓
【判断是否需要自愈】
    ├─ 有失败步骤 → 进入阶段4（自愈）
    └─ 全部通过 → 学习依赖关系到知识图谱
```

#### 3.3 Prompt

**系统提示词（场景测试结果分析专家）**：
```
你是一个场景测试结果分析专家。
你的任务是根据场景用例的执行结果，提供深入的分析报告。

## 分析内容

1. **执行概览**：总结整体执行情况
2. **失败步骤分析**：深入分析失败步骤的原因，包括：
   - HTTP状态码分析
   - 业务状态码分析
   - 错误信息解读
   - 可能的根因
3. **成功步骤评估**：评估成功步骤是否真正符合预期
4. **业务流程完整性**：评估整个业务流程是否完整执行
5. **改进建议**：提供具体的优化建议

## 输出格式

请以 JSON 格式返回分析结果：
{
    "overview": "整体执行情况概述",
    "failed_analysis": [
        {
            "step_order": 步骤序号,
            "api_path": "接口路径",
            "root_cause": "失败根因分析",
            "suggestions": "修复建议"
        }
    ],
    "success_evaluation": "成功步骤评估",
    "business_flow_completeness": "业务流程完整性评估",
    "improvement_suggestions": ["改进建议1", "改进建议2", ...]
}

请提供专业、深入的分析，帮助用户理解测试结果并改进测试用例。
```

#### 3.4 分析输出示例

**规则分析输出**：
```json
{
  "overall_status": "failed",
  "total_steps": 3,
  "passed_steps": 1,
  "failed_steps": 2,
  "analysis": [
    {
      "step_order": 1,
      "api_path": "/api/login",
      "status": "passed",
      "http_status": 200,
      "business_code": 0,
      "message": "登录成功"
    },
    {
      "step_order": 2,
      "api_path": "/api/orders",
      "status": "failed",
      "http_status": 200,
      "business_code": 4200,
      "message": "门店授权码无效，请联系相关人员",
      "failure_reason": "业务逻辑错误：门店授权码无效"
    }
  ],
  "summary": "场景执行失败：2个步骤失败，主要原因为门店授权码无效和参数缺失"
}
```

**AI分析输出**：
```json
{
  "overview": "场景执行失败：3个步骤中1个通过，2个失败。主要问题集中在业务逻辑错误和参数缺失。",
  "failed_analysis": [
    {
      "step_order": 2,
      "api_path": "/api/orders",
      "root_cause": "门店授权码无效，可能是授权码过期、配置错误或用户没有该门店的操作权限",
      "suggestions": "检查门店授权码配置，确认授权码是否有效，验证用户权限"
    },
    {
      "step_order": 3,
      "api_path": "/api/rooms",
      "root_cause": "缺少roomId参数，可能是参数映射配置错误，需要从步骤1的响应中提取roomId",
      "suggestions": "检查参数映射配置，确保从步骤1的响应中正确提取roomId并映射到步骤3"
    }
  ],
  "success_evaluation": "步骤1（登录）执行成功，返回了有效的token，为后续步骤提供了必要的鉴权信息。",
  "business_flow_completeness": "业务流程不完整：虽然登录成功，但后续的订单创建和房间查询都失败，无法完成完整的业务流程。",
  "improvement_suggestions": [
    "检查门店授权码配置，确保授权码有效",
    "完善参数映射配置，确保roomId等必要参数能正确传递",
    "验证用户权限，确保用户有操作该门店的权限",
    "检查业务逻辑，确认订单创建和房间查询的前置条件是否满足"
  ]
}
```


#### 3.3 知识图谱使用

**使用时机**：执行成功后（所有步骤的业务状态码都为0）

**使用方式**：
- 从test_cases表的steps字段中提取param_mappings
- 建立接口节点和依赖边
- 更新边的置信度（成功执行增加置信度，失败降低置信度）

**学习示例**：
```python
# 场景执行成功后的学习
步骤1: POST /api/login
步骤2: GET /api/user/profile
param_mappings: [
    {
        "from_step": 1,
        "from_field": "data.token",
        "to_field": "Authorization",
        "to_type": "headers"
    }
]

# 学习到知识图谱
节点: POST:/api/login
节点: GET:/api/user/profile
边: POST:/api/login → GET:/api/user/profile
    字段映射: {"Authorization": "data.token"}
    置信度: 0.85 (成功执行后增加)
```

---

### 四、阶段4：自愈修复（Healer Agent）

#### 4.1 职责与工作

**自愈专家Agent（Healer Agent）**负责：
1. **分析失败原因**：对失败的步骤进行根因分析
2. **判断是否可自愈**：判断失败类型是否可以自动修复
3. **自动修复**：对可自愈的失败进行自动修复
4. **更新测试用例**：将修复后的步骤更新到test_cases表

**重要特性**：
- **不影响场景用例生成**：自愈只修改执行失败的步骤，不影响前面阶段生成的场景用例结构
- **可重复执行**：修复后可以再次执行，如果还有失败可以再次自愈
- **智能判断**：只修复可自愈的失败类型，不可自愈的失败需要人工介入

#### 4.2 可自愈的失败类型

1. **接口路径变更**（可自愈）
   - 自动更新接口路径

2. **参数名称/请求体变更**（可自愈）
   - 自动更新参数名称或请求体结构

3. **响应结构或状态码变更**（可自愈）
   - 自动更新期望状态码或响应结构

4. **断言/期望配置错误**（可自愈）
   - 自动修正断言规则

5. **鉴权/Token失效或缺失**（可自愈）
   - 自动更新token映射或重新获取token

6. **业务逻辑错误或环境不可用**（不可自愈）
   - 需要人工介入，如门店授权码无效、参数值错误等

#### 4.3 工作流程

```
【阶段3分析结果：有失败步骤】
    ↓
【阶段4：自愈修复】
    ├─ 分析失败原因（Healer Agent）
    │   ├─ 提取失败步骤信息
    │   ├─ 分析失败类型
    │   ├─ 判断是否可自愈
    │   └─ 生成修复建议
    ├─ 判断是否可自愈
    │   ├─ 可自愈 → 执行自动修复
    │   └─ 不可自愈 → 返回人工介入提示
    ├─ 自动修复（如可自愈）
    │   ├─ 获取当前测试用例
    │   ├─ 获取最新API定义
    │   ├─ AI修复失败步骤
    │   └─ 更新test_cases表
    └─ 返回修复结果
    ↓
【修复后可再次执行】
    ↓
重新进入阶段3（执行与分析）
    ↓
如果还有失败 → 再次进入阶段4（自愈）
```

#### 4.4 Prompt

**系统提示词（Healer Agent）**：
```
你是一个接口测试自愈专家。
分析测试步骤失败的原因，并判断是否可以自动修复。

常见失败类型:
1. 接口路径变更 (可自愈)
2. 参数名称/请求体变更 (可自愈)
3. 响应结构或状态码变更 (可自愈，如期望 200 实际 201)
4. 断言/期望配置错误 (可自愈)
5. 鉴权/Token 失效或缺失 (可自愈：更新 token 映射)
6. 业务逻辑错误或环境不可用 (不可自愈，需人工介入)

返回 JSON 格式:
{
    "failure_type": "类型简述",
    "root_cause": "根本原因说明",
    "can_heal": true 或 false,
    "suggested_fix": "具体修复建议（人可读）",
    "patch_hint": "可选，结构化修复提示，如: 更新期望状态码为xxx / 更新请求头Authorization / 更新path 等"
}
```

#### 4.5 自愈示例

**场景**：步骤2执行失败，业务状态码为4200，错误信息为"门店授权码无效"

**自愈流程**：
```
1. 分析失败原因
   - 失败类型：业务逻辑错误
   - 根因：门店授权码无效
   - 可自愈：否（需要人工检查门店授权码）

2. 返回结果
   {
     "failure_type": "业务逻辑错误",
     "root_cause": "门店授权码无效，可能是授权码过期或配置错误",
     "can_heal": false,
     "suggested_fix": "请检查门店授权码配置，确认授权码是否有效",
     "patch_hint": null
   }

3. 提示用户人工介入
```

**另一个示例**：步骤2执行失败，HTTP状态码为404，接口路径变更

**自愈流程**：
```
1. 分析失败原因
   - 失败类型：接口路径变更
   - 根因：接口路径从 /api/v2/orders 变更为 /api/v3/orders
   - 可自愈：是

2. 自动修复
   - 获取最新API定义
   - 更新步骤2的api_path为 /api/v3/orders
   - 保存到test_cases表

3. 返回修复结果
   {
     "status": "healed",
     "message": "已自动修复接口路径",
     "changes": [
       {
         "step_order": 2,
         "field": "api_path",
         "old_value": "/api/v2/orders",
         "new_value": "/api/v3/orders"
       }
     ]
   }

4. 提示用户重新执行
```

---

### 五、场景用例生成完整流程

```
用户输入："为登录并获取用户信息接口生成完整的场景测试"
    ↓
【阶段1：场景理解】NLU Agent
    - 解析自然语言描述
    - 提取intent, entities, actions, expected_results
    - 保存到scenarios表
    ↓
【阶段2：场景编排】ScenarioParser
    ├─ API检索（向量/关键词）
    ├─ 知识图谱增强（查询依赖边）
    ├─ AI编排生成（生成steps）
    └─ 步骤增强（合并headers、注入Schema）
    ↓
保存到test_cases表
    ↓
【阶段3：测试执行与结果分析】
    ├─ 子阶段1：测试执行
    │   ├─ 按步骤顺序执行
    │   ├─ 处理参数映射
    │   └─ 收集执行结果
    ├─ 子阶段2：结果分析
    │   ├─ 检查HTTP状态码
    │   ├─ 检查业务状态码（code字段）
    │   ├─ 提取错误信息（message字段）
    │   └─ 生成分析报告
    └─ 判断整体状态
        ├─ 全部通过 → 学习依赖关系到知识图谱
        └─ 有失败 → 进入阶段4（自愈）
    ↓
【阶段4：自愈修复】（如有失败）
    ├─ 分析失败原因
    ├─ 判断是否可自愈
    ├─ 自动修复（如可自愈）
    └─ 更新test_cases表
    ↓
【修复后可再次执行】
    ↓
重新进入阶段3（执行与分析）
```

---

### 六、知识图谱在场景用例中的使用总结

#### 6.1 使用阶段

1. **场景编排阶段（生成时）**：
   - 查询与候选API相关的依赖边
   - 将依赖关系注入到Prompt中
   - 大模型参考知识图谱生成更准确的步骤序列

2. **执行后学习阶段（学习时）**：
   - 场景执行成功后（所有步骤业务状态码为0），提取param_mappings
   - 建立接口节点和依赖边
   - 更新置信度

#### 6.2 知识图谱的价值

1. **减少AI幻觉**：通过历史验证的依赖关系，减少大模型生成错误的依赖
2. **提高准确性**：优先使用高置信度的依赖关系
3. **形成闭环**：执行即学习，越用越准
4. **智能编排**：帮助大模型理解接口间的真实依赖关系

---

### 七、关键特性

1. **智能检索**：优先向量语义检索，备选关键词排序
2. **知识图谱增强**：在编排时注入历史依赖关系，提高准确性
3. **自动依赖识别**：自动识别参数依赖和鉴权映射
4. **步骤增强**：自动合并headers、补齐动态头映射
5. **业务状态码判断**：通过检查业务状态码（code字段）判断执行是否成功
6. **智能分析**：自动分析失败原因，提取错误信息（message字段）
7. **自愈修复**：对可自愈的失败进行自动修复，不影响场景用例生成
8. **学习闭环**：执行成功后自动学习，形成"越用越准"的正向循环

---

## 总结

场景用例生成采用**四阶段架构**：

1. **阶段1：场景理解（NLU Agent）** - 解析自然语言，提取结构化信息
2. **阶段2：场景编排（ScenarioParser）** - API检索、知识图谱增强、AI编排生成、步骤增强
3. **阶段3：测试执行与结果分析** - 执行测试步骤，分析业务状态码，判断是否通过
4. **阶段4：自愈修复（Healer Agent）** - 分析失败原因，自动修复可自愈的失败

**核心特点**：
- **知识图谱深度集成**：在场景编排阶段提供依赖参考，执行成功后自动学习
- **业务状态码判断**：通过检查所有步骤的业务状态码（code=0）判断是否通过
- **智能分析**：自动分析失败原因，提取message等错误信息
- **自愈机制**：对可自愈的失败进行自动修复，不影响前面阶段生成的场景用例
- **学习闭环**：执行成功后自动学习依赖关系，形成"越用越准"的正向循环

与接口用例生成不同，场景用例生成**深度依赖知识图谱**，知识图谱不仅提供依赖参考，还参与学习闭环，是场景用例生成的核心技术之一。
